extends layout

block content 
    .main
        .form-content 
            h2.title-form Connection
            form.form-signin
                input.form-control-signin#email(type="email", placeholder="Adresse mail", required, autofocus)
                input.form-control-signin#password(type="password", placeholder="Mot de passe", required )
                br
                button.btn.btn-login(type="submit") Connection